/*
  Secrets.h — Medina County Career Center

  PURPOSE:
  Stores sensitive information (like WiFi credentials) separately
  from the main program.

  WHY THIS FILE EXISTS:
  - Keeps passwords out of the main code
  - Prevents accidentally sharing credentials (ex: GitHub)
  - Makes it easy to change networks without editing the main program

  HOW IT WORKS:
  - This file is included using: #include "Secrets.h"
  - The #define values act like constants in the main code
*/

#pragma once                   // ensures this file is only included once (prevents duplicate definitions)

// WiFi credentials — get these from your instructor
#define WIFI_SSID "YourNetworkName"
// defines WIFI_SSID as the network name (text replacement at compile time)

#define WIFI_PASS "YourNetworkPassword"
// defines WIFI_PASS as the network password (also replaced at compile time)
